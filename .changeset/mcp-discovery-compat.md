---
"@upstash/context7-mcp": patch
---

Advertise path-aware protected-resource metadata and a `/mcp/server-card`, restore `get-library-docs` as a `query-docs` alias, stop forwarding Cursor IDE bearer tokens to the REST API, and stop logging expected cap-0 subscription refusals.
