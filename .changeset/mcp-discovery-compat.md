---
"@upstash/context7-mcp": patch
---

Advertise path-aware protected-resource metadata and a `/mcp/server-card`, redirect `get-library-docs` calls to `query-docs` without advertising another tool, stop forwarding Cursor IDE bearer tokens to the REST API, and stop logging expected cap-0 subscription refusals.
